import { createTheme } from '@mui/material/styles';
const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#422040',
        },
        secondary: {
            main: '#E57A44',
        },
    }
});

export default theme;
